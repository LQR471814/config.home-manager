#!/bin/sh

trap 'kill 0' EXIT

notify-user() {
	while $(exit 0); do
		notify-send -t 5000 -e "Idle inhibited."
		sleep 20
	done
}

notify-user &

systemd-inhibit --what=idle --who=manual --why='preventing sleep' sleep infinity

